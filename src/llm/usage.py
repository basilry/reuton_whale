"""Cost calculator for LLM providers. Filled in TRACK 1."""
from __future__ import annotations


def estimate_cost(provider: str, model: str, tokens_in: int, tokens_out: int) -> float:
    """Return USD cost estimate. TRACK 1 will populate the rate table."""
    return 0.0
