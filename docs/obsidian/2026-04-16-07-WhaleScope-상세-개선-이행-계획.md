---
type: improvement-plan
project: 02015-WhaleScope
date: 2026-04-16
sequence: 7
status: in-progress
assignment: Wrtn Technologies Product Engineer
related:
  - 2026-04-16-WhaleScope-리뷰기반-개선안.md
  - 2026-04-16-WhaleScope-다관점-코드리뷰-및-개선내역.md
  - 2026-04-16-WhaleScope-개선안-적용내역.md
tags:
  - cowork-session
  - whalescope
  - improvement-plan
---

# WhaleScope 상세 개선 이행 계획

> 기존 리뷰 문서의 Finding 목록을 참조하되, 이 문서는 **파일:라인:함수:코드 변경** 수준의 구체적 이행 명세에 집중한다.

## 1. 미커밋 변경사항 정리 및 커밋 (Phase 0)

### 1.1 현황
- 24개 파일 수정 중 (git status 기준)
- P1 Finding 5개 반영 완료 상태이나 커밋 전

### 1.2 이행
```bash
# 커밋 단위 분리 (논리적 단위별)
git add src/main.py src/ingestion/telethon_listener.py src/ingestion/normalizer.py src/signals/engine.py src/storage/sheets_client.py src/storage/protocol.py
git commit -m "feat(pipeline): integrate Telegram whale events into signal pipeline"

git add apps/dashboard/app/api/ apps/dashboard/lib/auth.ts apps/dashboard/middleware.ts
git commit -m "feat(dashboard): add API authentication and sanitize error responses"

git add apps/dashboard/app/insights/ apps/dashboard/components/ apps/dashboard/app/page.tsx apps/dashboard/app/globals.css
git commit -m "feat(dashboard): add user insights page and improve UI"

git add scripts/ tests/ README.md .env.example .gitignore
git commit -m "chore: update scripts, tests, docs"
```

---

## 2. P0 이슈 해소 (즉시)

### 2.1 운영 알림 센터 raw JSON 노출

**문제 위치**:
- `apps/dashboard/lib/metrics.ts:188-205` - `normalizeSystemLogRows()`
- `apps/dashboard/lib/metrics.ts:200` - `row.errors` 문자열 보간에 JSON 그대로 삽입
- `apps/dashboard/lib/metrics.ts:109-125` - `normalizeLatestRun()` details 필드 미파싱
- `apps/dashboard/components/system-log-panel.tsx:75` - `row.message` 그대로 렌더링

**수정 전략**:

#### A. `metrics.ts` - `normalizeSystemLogRows()` (라인 196-201)
```typescript
// Before (라인 200):
message: row.details || (errorCount > 0 ? `${errorCount} error(s): ${row.errors}` : "No details recorded."),

// After:
message: humanizeLogMessage(row) || (errorCount > 0 ? `${errorCount}건 오류 발생` : "정상 완료"),
```

#### B. `metrics.ts` - 새 함수 `humanizeLogMessage()` 추가
```typescript
function humanizeLogMessage(row: SystemLogRow): string | null {
  if (!row.details && !row.errors) return null;

  const details = parseJsonSafe(row.details);
  if (typeof details === 'object' && details !== null) {
    const parts: string[] = [];
    if (details.stage) parts.push(`[${details.stage}]`);
    if (details.message) parts.push(details.message);
    if (details.count) parts.push(`(${details.count}건 처리)`);
    return parts.join(' ') || null;
  }

  if (typeof row.details === 'string') {
    return row.details.length > 120 ? row.details.slice(0, 120) + '...' : row.details;
  }
  return null;
}
```

#### C. `system-log-panel.tsx` - 렌더링 보호 (라인 75)
```typescript
// Before:
<p>{row.message}</p>

// After:
<p>{typeof row.message === 'string' ? row.message : '상세 로그 확인 필요'}</p>
```

**검증**: 대시보드 `/` 접속 -> 운영 알림 센터에서 JSON 문자열 미노출 확인

---

### 2.2 서비스 카드 버튼 미동작

**문제 위치**:
- `apps/dashboard/components/metric-card.tsx:1-16` - 버튼 없음
- `apps/dashboard/components/brief-panel.tsx:71-174` - 액션 버튼 없음
- `apps/dashboard/components/system-log-panel.tsx:52-82` - 로그 상세보기 없음

**수정 전략**: 채용 과제 범위상 실제 백엔드 트리거 연결보다는 **시각적 인터랙션 + 피드백**에 집중

#### A. `metric-card.tsx` - 액션 버튼 추가
```typescript
interface MetricCardProps {
  title: string;
  value: string | number;
  subtitle?: string;
  status?: 'healthy' | 'warning' | 'error';
  actions?: Array<{ label: string; onClick: () => void }>;
}

{props.actions?.map((action) => (
  <button key={action.label} onClick={action.onClick} className="card-action-btn">
    {action.label}
  </button>
))}
```

#### B. `page.tsx` - MetricCard에 actions prop 전달
```typescript
<MetricCard
  title="Pipeline"
  value={data.pipelineStatus}
  actions={[
    { label: "로그 보기", onClick: () => scrollToSection('system-log') },
    { label: "상세", onClick: () => setSelectedPanel('pipeline') },
  ]}
/>
```

#### C. `system-log-panel.tsx` - 로그 항목 클릭 시 상세 모달
```typescript
const [selectedLog, setSelectedLog] = useState<SystemLog | null>(null);

<div onClick={() => setSelectedLog(log)} className="cursor-pointer hover:bg-surface-alt">
  ...
</div>

{selectedLog && (
  <div className="log-detail-modal">
    <h3>{selectedLog.title}</h3>
    <pre>{JSON.stringify(selectedLog, null, 2)}</pre>
    <button onClick={() => setSelectedLog(null)}>닫기</button>
  </div>
)}
```

**검증**: 각 버튼 클릭 시 스크롤/모달 동작 확인

---

## 3. 이전 Major Finding 해소 (P1 - 핵심)

### 3.1 Major 2: `_signals_to_top5()` signal-first 모델 미적용

**문제 위치**:
- `src/main.py` - `_signals_to_top5()` 함수
- signal에 연결된 트랜잭션 해시가 비어있거나 USD 값이 0인 경우가 저장됨

**근본 원인**: Signal 생성 시점에 Event의 tx_hash와 amount_usd를 복사하지 않는 경로 존재

**수정 전략**:

#### A. `src/signals/engine.py` - Signal 생성 시 Event 필드 보장
```python
def _create_signal(self, events: list[Event], rule_name: str, ...) -> Signal:
    primary = events[0]
    return Signal(
        rule=rule_name,
        tx_hash=primary.tx_hash or "unknown",
        amount_usd=primary.amount_usd if primary.amount_usd > 0 else self._estimate_usd(primary),
    )
```

#### B. `src/main.py` - `_signals_to_top5()` 필터링 강화
```python
def _signals_to_top5(signals: list[Signal]) -> list[Signal]:
    valid = [s for s in signals if s.tx_hash and s.tx_hash != "unknown" and s.amount_usd > 0]
    valid.sort(key=lambda s: s.score, reverse=True)
    return valid[:5]
```

**검증**: `pytest tests/test_main.py -k "top5"` + signal에 빈 hash/0 USD 없는지 확인

---

### 3.2 Major 3: Telegram 개인화가 LLM brief_text 우회

**문제 위치**:
- `src/distributor/telegram_bot.py` - 사용자별 알림 발송 로직
- `src/distributor/formatters.py` - 메시지 포맷팅

**근본 원인**: Watchlist 기반 필터링 후 사용자에게 보내는 메시지가 LLM 생성 brief_text가 아닌, 원시 signal 데이터를 직접 포맷팅

**수정 전략**:

#### A. `src/distributor/formatters.py` - brief_text 우선 사용
```python
def format_user_alert(signal: Signal, brief_text: str | None = None) -> str:
    if brief_text:
        return f"whale *WhaleScope Alert*\n\n{brief_text}\n\n_{signal.rule} | {signal.amount_usd:,.0f} USD_"
    return _format_raw_signal(signal)
```

#### B. `src/distributor/telegram_bot.py` - brief_text 전달 경로 확보
```python
async def _send_daily_to_user(self, user_id: int, signals: list[Signal], briefs: dict[str, str]):
    for sig in signals:
        brief_text = briefs.get(sig.tx_hash)
        message = format_user_alert(sig, brief_text)
        await self._bot.send_message(chat_id=user_id, text=message, parse_mode="Markdown")
```

#### C. `src/main.py` - brief_text를 distributor에 전달
```python
# Stage 7 (Distribution):
briefs = {entry["tx_hash"]: entry["brief_text"] for entry in daily_brief_rows}
await distributor.send_daily(top5_signals, briefs)
```

**검증**: Telegram 테스트 채널에서 수신된 메시지가 LLM 한국어 해설을 포함하는지 확인

---

### 3.3 Major 4: analysis_log telemetry schema 미정합

**문제 위치**:
- `src/llm/usage.py` - 토큰 사용량 추적
- `src/storage/schema.py` - analysis_log 탭 스키마 정의
- `src/analyzer/claude_analyzer.py` - LLM 호출 후 usage 기록

**근본 원인**: Gemini 2.5 모델 비용 단가 미등록, analysis_log 스키마와 실제 기록 필드 불일치

**수정 전략**:

#### A. `src/llm/usage.py` - Gemini 2.5 비용 추가
```python
COST_PER_1K = {
    "claude-3-5-sonnet": {"input": 0.003, "output": 0.015},
    "claude-3-haiku": {"input": 0.00025, "output": 0.00125},
    "gemini-2.0-flash": {"input": 0.0001, "output": 0.0004},
    "gemini-2.5-flash": {"input": 0.00015, "output": 0.0006},
    "gemini-2.5-pro": {"input": 0.00125, "output": 0.005},
    "llama-3.3-70b": {"input": 0.0, "output": 0.0},
}
```

#### B. `src/storage/schema.py` - analysis_log 헤더 정합
```python
ANALYSIS_LOG_HEADERS = [
    "run_id", "timestamp", "provider", "model",
    "input_tokens", "output_tokens", "cost_usd",
    "task", "latency_ms", "status",
]
```

#### C. `src/analyzer/claude_analyzer.py` - 기록 시 누락 필드 보완
```python
def _log_usage(self, result: LLMResult, task: str, latency_ms: float):
    self._storage.append_analysis_log({
        "run_id": self._run_id,
        "timestamp": now_iso(),
        "provider": result.provider,
        "model": result.model,
        "input_tokens": result.input_tokens,
        "output_tokens": result.output_tokens,
        "cost_usd": calculate_cost(result.model, result.input_tokens, result.output_tokens),
        "task": task,
        "latency_ms": round(latency_ms, 1),
        "status": "ok",
    })
```

**검증**: `python -m src.main --dry-run` 실행 후 analysis_log 탭에 모든 필드 정상 기록 확인

---

## 4. High Finding 해소 (6개)

### 의존성 그래프

```
H1 (_tg_direction dead code) ---+
H5 (Protocol 타입 어노테이션) ---+-- 독립 (병렬 가능)
H4 (auth timing-safe) ----------+

H2 (_parse_dt 중복) ---- 독립

H3 (Telethon run 테스트) ---+
H6 (heartbeat 순서) --------+-- 의존 (H6 먼저 수정 후 H3 테스트 작성)
```

### 4.1 H1: `_tg_direction` dead code + exchange-to-exchange 방향 미정의

**파일**: `src/ingestion/telethon_listener.py`

```python
def _tg_direction(from_label: str, to_label: str) -> str:
    from_is_exchange = "exchange" in from_label.lower()
    to_is_exchange = "exchange" in to_label.lower()
    if from_is_exchange and to_is_exchange:
        return "exchange-to-exchange"
    if from_is_exchange:
        return "outflow"
    if to_is_exchange:
        return "inflow"
    return "transfer"
```

### 4.2 H2: `_parse_dt` 3곳 중복 정의

**파일들**: `src/storage/queries.py`, `src/main.py`, `src/analyzer/claude_analyzer.py`

**수정**: `src/utils/datetime_utils.py`로 통합
```python
from datetime import datetime, timezone

def parse_dt(value: str) -> datetime:
    for fmt in ("%Y-%m-%dT%H:%M:%S%z", "%Y-%m-%dT%H:%M:%S.%f%z", "%Y-%m-%d %H:%M:%S"):
        try:
            dt = datetime.strptime(value, fmt)
            return dt.astimezone(timezone.utc) if dt.tzinfo else dt.replace(tzinfo=timezone.utc)
        except ValueError:
            continue
    raise ValueError(f"Unparseable datetime: {value}")
```
- 3곳 모두 `from src.utils.datetime_utils import parse_dt`로 교체

### 4.3 H3: Telethon `run()` 메서드 테스트 부재

**파일**: `tests/test_telethon_listener.py` (신규 또는 기존 확장)

H6 수정 후 작성:
```python
@pytest.mark.asyncio
async def test_handle_message_with_valid_whale_alert():
    storage = MockSheetsClient()
    listener = TelethonListener(
        api_id=0, api_hash="test", session="test",
        storage=storage, router=None, channel="test"
    )
    await listener._handle_message(
        msg_id=1,
        msg_date=datetime.now(timezone.utc),
        text="whale 500,000 #USDT transferred from unknown wallet to #Binance"
    )
    assert len(storage.tg_whale_events) == 1
    assert storage.tg_whale_events[0]["blockchain"] == "ethereum"

@pytest.mark.asyncio
async def test_handle_message_unparseable():
    storage = MockSheetsClient()
    listener = TelethonListener(...)
    await listener._handle_message(1, datetime.now(timezone.utc), "hello world")
    assert len(storage.tg_whale_events) == 0
```

### 4.4 H4: auth.ts timing-safe comparison 미사용

**파일**: `apps/dashboard/lib/auth.ts`

```typescript
import { timingSafeEqual } from 'crypto';

export function verifyDashboardPassword(provided: string): boolean {
  const expected = process.env.DASHBOARD_PASSWORD;
  if (!expected) return false;
  if (provided.length !== expected.length) return false;

  const a = Buffer.from(provided);
  const b = Buffer.from(expected);
  return timingSafeEqual(a, b);
}
```

### 4.5 H5: Protocol `list_tg_whale_events` 타입 어노테이션 누락

**파일**: `src/storage/protocol.py`

```python
from typing import Protocol
from datetime import datetime

class StorageProtocol(Protocol):
    def list_tg_whale_events(self, since: datetime | None = None) -> list[dict[str, str]]:
        ...
```

### 4.6 H6: listener start heartbeat가 `client.connect()` 전에 기록

**파일**: `src/ingestion/telethon_listener.py` - `run()` 메서드

```python
async def run(self) -> None:
    from telethon import TelegramClient, events
    client = TelegramClient(self._session, self._api_id, self._api_hash)

    @client.on(events.NewMessage(chats=self._channel))
    async def _handler(event):
        await self._handle_message(event.id, event.date, event.raw_text or "")

    async with client:
        self._record_heartbeat("listener_started")  # connect 성공 후로 이동
        logger.info("Listening on channel=%r", self._channel)
        await client.run_until_disconnected()
```

---

## 5. P2 이슈 해소

### 5.1 Event loop blocking 해소

**문제 파일**:
- `src/ingestion/telethon_listener.py:99-110` - `_router.call_task()` 동기 호출 (2-3초 차단)
- `src/storage/sheets_client.py:548-563` - `append_tg_whale_event()` 동기 Sheets I/O (200-500ms 차단)

**수정**: `asyncio.to_thread()` wrapper 적용

```python
async def _handle_message(self, msg_id: int, msg_date, text: str) -> None:
    parsed = parse_tg_message(text)
    confidence = "high"

    if parsed is None and self._router is not None:
        try:
            result = await asyncio.to_thread(
                self._router.call_task,
                "nl_intent",
                system="Extract whale transaction details...",
                user=text,
            )
            parsed = json.loads(result.text)
            confidence = "low"
        except Exception as exc:
            logger.warning("LLM fallback failed msg_id=%s: %s", msg_id, exc)

    if parsed is None:
        return

    event_row = { ... }

    try:
        await asyncio.to_thread(self._storage.append_tg_whale_event, event_row)
    except Exception as exc:
        logger.error("Storage failed msg_id=%s: %s", msg_id, exc)
```

**검증**: listener 실행 중 연속 메시지 처리 시 blocking 없이 처리되는지 로그 확인

### 5.2 Listener health monitoring 추가

**문제 파일**:
- `scripts/run_listener.py:49-78` - 초기화 후 상태 감시 없음
- `src/storage/sheets_client.py:74-82` - 초기화 시점에만 Sheets 확인

**수정**:

#### A. `src/ingestion/telethon_listener.py` - heartbeat 메커니즘
```python
class TelethonListener:
    def __init__(self, ...):
        ...
        self._last_message_at: datetime | None = None
        self._message_count: int = 0
        self._error_count: int = 0

    def health_status(self) -> dict:
        now = datetime.now(timezone.utc)
        last = self._last_message_at
        staleness = (now - last).total_seconds() if last else None
        return {
            "status": "healthy" if staleness and staleness < 900 else "stale",
            "last_message_at": last.isoformat() if last else None,
            "staleness_seconds": staleness,
            "message_count": self._message_count,
            "error_count": self._error_count,
        }

    async def _handle_message(self, ...):
        self._last_message_at = datetime.now(timezone.utc)
        self._message_count += 1
        # ... 기존 로직
```

#### B. `scripts/run_listener.py` - 주기적 heartbeat 로깅
```python
async def _run():
    ...
    listener = TelethonListener(...)

    async def _heartbeat_loop():
        while True:
            await asyncio.sleep(300)  # 5분마다
            status = listener.health_status()
            logger.info("Listener health: %s", status)
            if status["status"] == "stale":
                logger.warning("Listener stale for %ss", status["staleness_seconds"])

    asyncio.create_task(_heartbeat_loop())
    await listener.run()
```

---

## 6. Medium Finding 선별 해소 (과제 제출 직결 항목만)

### 6.1 `cleanGeneratedBrief` 중복 정의
- `apps/dashboard/app/page.tsx`와 `apps/dashboard/app/insights/page.tsx`에 동일 함수
- **수정**: `apps/dashboard/lib/format.ts`로 이동, 양쪽에서 import

### 6.2 `_safe_float` 실패 시 무경고 0.0 반환
- `src/ingestion/normalizer.py` 또는 관련 헬퍼
- **수정**: `_safe_float(value, field_name)` 시그니처로 변경, 실패 시 `logger.debug` 추가

### 6.3 `list_tg_whale_events` edge case
- since=None, 빈 시트, timezone 불일치
- **수정**: 테스트 추가 (`tests/test_storage_tg_events.py`)

---

## 7. 검증 체크리스트

### 7.1 자동 테스트
```bash
pytest tests/ -v
pytest tests/test_telethon_listener.py -v
pytest tests/test_storage_tg_events.py -v
pytest tests/test_main.py -k "top5 or tg" -v
cd apps/dashboard && npx tsc --noEmit
```

### 7.2 수동 검증
```bash
python scripts/smoke_pipeline.py
cd apps/dashboard && npm run dev
# / 접속: raw JSON 미노출, 버튼 동작 확인
# /insights 접속: 내부 에러 미노출 확인
curl http://localhost:3000/api/dashboard                                        # 401
curl -H "x-dashboard-password: wrong" http://localhost:3000/api/dashboard       # 401
curl -H "x-dashboard-password: $DASHBOARD_PASSWORD" http://localhost:3000/api/dashboard  # 200
```

### 7.3 배포 전 확인
```bash
cd apps/dashboard && npm run build
python -m py_compile src/main.py
python -m py_compile src/ingestion/telethon_listener.py
```

---

## 8. 우선순위별 실행 순서

| 순서 | 작업 | 섹션 | 예상 소요 | 위험도 |
|------|------|------|---------|--------|
| 1 | 미커밋 변경사항 커밋 | 1 | 30분 | 낮음 |
| 2 | raw JSON 노출 수정 | 2.1 | 1시간 | 낮음 |
| 3 | 서비스 카드 버튼 | 2.2 | 2시간 | 낮음 |
| 4 | Major 2: signal-first | 3.1 | 3시간 | 중간 |
| 5 | Major 3: TG 개인화 LLM | 3.2 | 3시간 | 중간 |
| 6 | Major 4: telemetry | 3.3 | 1시간 | 낮음 |
| 7 | High Finding 6개 | 4 | 4시간 | 낮음-중간 |
| 8 | Event loop blocking | 5.1 | 2시간 | 중간 |
| 9 | Health monitoring | 5.2 | 2시간 | 낮음 |
| 10 | Medium 선별 3개 | 6 | 2시간 | 낮음 |
| **합계** | | | **~20시간** | |

**크리티컬 패스**: 1 -> 2 -> 4 -> 5 -> 3 (과제 제출 필수선)
**나머지**: 병렬 가능
