---
type: project-report
project: WhaleScope
date: 2026-04-19
sequence: 8
version: v6
status: completed
tags:
  - WhaleScope
  - v6
  - 개선완료
  - 데이터파이프라인
  - UX
  - 인프라모니터링
---

# WhaleScope v6 개선 완료 보고서

## 개요

2026-04-18 세션에서 수립한 v6 개선계획(데이터파이프라인/UX/인프라모니터링)을 2026-04-19 세션에서 전량 구현·검증·배포 완료했다. 총 6개 커밋(backend observability, admin Render 대시보드, Batch A UX, B.1 mood strip, B.3 Suspense, B.2 hybrid brief)이 origin/main에 반영되었으며 dashboard typecheck/lint 통과, 402 pytest 전건 통과 상태다.

## 커밋 타임라인

| 커밋 | 범주 | 메시지 |
|------|------|--------|
| `5c9b0f1` | backend | feat(observability): expand service_health v2 with instance/job telemetry |
| `e95ff27` | admin | feat(admin): add Render API observability dashboard |
| `79d837d` | UX(Batch A) | feat(dashboard): tighten watchlist, equalize story heights, add standby live state |
| `80f9c06` | UX(B.1) | feat(dashboard): promote market mood above brief as full-width hero strip |
| `cc303f7` | 운영(B.3) | fix(admin): wrap async data loading in Suspense boundary |
| `4499976` | 파이프라인(B.2) | feat(brief): hybrid full/incremental briefing with RSS + curation context |

## 작업 상세

### 1. Service Health v2 (commit `5c9b0f1`)

- `src/observability/service_health.py`: heartbeat 스키마 확장
  - 신규 필드: `instance_id`, `job_name`, `last_success_at`, `last_failure_at`, `processed_count`, `lag_seconds`, `duration_ms`, `source_name`
  - 헬퍼 함수: `_normalize_timestamp`, `_normalize_optional_int`, `_default_job_name`, `resolve_instance_id`, `coalesce_source_names`, `derive_status_timestamps`
- `src/pipeline/run_all.py`: `_JOB_SOURCE_NAMES` 맵 + `_processed_count` 헬퍼 + 확장된 `_record_job_heartbeat`
- 파이프라인 전 영역(`brief.py`, `broadcast_daily.py`, `broadcast_periodic.py`, `channel_health.py`, `signals.py`, `stories.py`, `news_rss.py`, `telethon_listener.py`)에 신규 필드 전파
- `src/storage/schema.py`에 `SERVICE_HEALTH_V2_HEADERS` + `ServiceHealthSheetRow` 인터페이스 정의
- 테스트 6개 파일 업데이트, 96건 통과

### 2. Admin Render Observability (commit `e95ff27`)

- `apps/dashboard/lib/env.ts`: `RenderEnv`, `RenderEnvState`, `getRenderEnv`, `getRenderEnvState`
- `apps/dashboard/lib/types.ts`: `RenderServiceKey`, `AdminRenderService/Deploy/Instance/LogLine`, `RenderApiError` 유니온, `AdminRenderObservability`, `ServiceHealthRow`
- `apps/dashboard/lib/render.ts` (신규): Render REST 클라이언트(timeout/retry/로그 윈도우 캐시)
  - `RENDER_API_BASE`, `RENDER_DEFAULT_TIMEOUT_MS=5000`, `RENDER_LOG_TIMEOUT_MS=10000`, `RENDER_LOG_WINDOW_MINUTES=15`, `RENDER_LOG_LIMIT=50`
- `apps/dashboard/app/admin/page.tsx`: `SignalActionCard`/`SystemLogPanel` 구조를 `AdminOperationsOverview`로 통합. 섹션 빌더 분리(`buildHero`, `buildDataSection`, `buildWorkerSection`, `buildRenderSection`, `buildRenderSectionFromObservability`, `buildCorrelationSection`)
- `apps/dashboard/components/admin/admin-operations-overview.tsx` (신규)

### 3. Batch A UX 폴리싱 (commit `79d837d`)

- `curated-watchlist-panel.tsx`: 42×42 원형 아바타 → pill(min-width 48, height 32) + `symbol.slice(0,4).toUpperCase()`. note/chain/lastSeenAt을 카드에서 제거하고 상세 모달로 이동
- `insights.module.css`: `.watchStoryRow` grid `5fr 7fr → 7fr 5fr`, `align-items: stretch` + 자식 `min-height: 100%`
- `whale-story-detail-modal.module.css`: padding `md → sm md`, gap `md → sm`, radius `lg → md`. 타이틀 `line-clamp: 1`, 본문 `line-clamp: 2 + text-xs`
- `whale-story-panel.tsx`: `visibleStories = stories.slice(0, 4)` 로 고정 4카드, 모달은 전체 배열 참조
- `live-updates-controller.tsx`: `LiveUpdateStatus`에 `"standby"` 추가. 라벨 ko `"실시간 비활성"` / en `"Live standby"`. tone 매핑 connected=good / offline=bad / else=warn. Redis·토큰 미설정을 disabled가 아닌 standby로 분리

### 4. B.1 공포탐욕 레이아웃 (commit `80f9c06`)

- `apps/dashboard/app/page.tsx`: bentoGrid 내 JSX를 `moodCard → heroCard` 순으로 재배열. `FearGreedGauge`를 `.moodGaugeCol`로 래핑
- `apps/dashboard/app/insights/insights.module.css` desktop(>=768px):
  - `.heroCard { grid-column: span 12 }`
  - `.moodCard { grid-column: span 12; flex-direction: row; align-items: stretch; gap: var(--space-xl) }`
  - `.moodCard > .moodGaugeCol { flex: 0 0 clamp(280px, 32%, 420px) }`
  - `.moodCard > .moodSummary { flex: 1 1 auto; justify-content: center }`
  - `.moodCard .moodDesc { max-width: 64ch }`
- 모바일은 기존 세로 스택 유지

### 5. B.3 운영 페이지 Suspense 경계 (commit `cc303f7`)

- `apps/dashboard/app/admin/page.tsx`에 `import { Suspense }` 추가
- 비동기 데이터 로딩 로직을 내부 `AdminOperationsBody` 서버 컴포넌트로 추출
- `AdminOperationsFallback` ("운영 스냅샷을 불러오는 중…") 도입
- 최상위 렌더 트리를 `<Suspense fallback={<AdminOperationsFallback />}>`로 감쌌다
- React DevTools v6.x의 "cleaning up async info that was not on the parent Suspense boundary" 경고 원인 해소. 앱 코드 버그는 아니나 명시적 경계로 완화

### 6. B.2 브리핑 하이브리드 전략 (commit `4499976`)

- `src/pipeline/brief.py`:
  - `BriefMode = Literal["full", "incremental"]`
  - `_FULL_BRIEF_HOURS_KST = frozenset({9, 15, 21})`, `_KST = ZoneInfo("Asia/Seoul")`
  - `_RSS_NEWS_TOP_N = int(os.environ.get("RSS_NEWS_TOP_N", "20"))`
  - `_BRIEF_LOG_DIR = repo_root / "data" / "brief_logs"`
  - 신규 헬퍼: `_is_full_slot`, `_full_brief_log_path`, `_save_full_brief_log`, `_load_latest_full_brief_log`, `_build_news_context`, `_build_curated_context`
  - `_build_brief_request` 키워드 전용 파라미터 `mode`, `news_rows`, `curated_rows`, `prior_brief` 추가
  - `run_brief_pipeline` 분기: `brief_mode = "full" if (_is_full_slot(now) or prior_brief is None) else "incremental"`
  - full 시 최근 24h RSS top N + curated watchlist 로드, JSONL 로그 적재
- `src/analyzer/prompt_loader.py`: `load_prompt(..., mode: str = "")` → `"name.mode.role"` 시도 후 `"name.role"` fallback
- `src/storage/sheets_client.py`: `list_news_feed(since, limit)` 메서드 신설
- `prompts/daily_brief.{full,incremental}.{system,user}.{txt,en.txt,ja.txt}` 총 12개 신규 프롬프트 추가, 기존 `daily_brief.{system,user}.txt`는 fallback 유지
- `tests/test_brief_fallback.py`: `_FakeSheets`에 `news_rows`, `curated_wallet_rows` 생성자 및 `list_news_feed()` / `list_curated_wallets()` 추가. 기존 5 테스트 패치 + 신규 10 테스트 → 총 402건 pytest 통과

## 비용 분석 (업데이트)

- 기존 추정(매 사이클 full): Sonnet 기준 브리핑당 ≈ $0.03, 24 사이클/일 × 30일 ≈ **$21/월**
- 적용된 하이브리드(일 3회 full + 21 incremental): **≈ $9/월**
- Haiku 혼합 라우팅 적용 시 절반 이하로 추가 절감 가능

## 검증

- `yarn dashboard:typecheck`: pass
- `yarn dashboard:lint`: pass
- `python3 -m pytest tests/ -q`: **402 passed** (warnings 3건은 PTB 22.2 deprecated notice)
- 원격 반영: `origin/main` 최신 커밋 `4499976`

## 후속 검토 항목

- [ ] RSS news 컨텍스트 선택 로직을 published_at 기반 → 점수 기반 정렬로 개선
- [ ] LLM 라우터가 `max_tokens` 파라미터를 받도록 인터페이스 확장 검토
- [ ] 브리핑 비용 절감을 위한 Haiku 혼합 라우팅 실측
- [ ] `data/brief_logs/` 보관 기간 정책(예: 14일 이후 자동 정리) 정의

## 관련 파일 (최종 상태)

- `src/pipeline/brief.py` (수정)
- `src/analyzer/prompt_loader.py` (수정)
- `src/storage/sheets_client.py` (수정)
- `src/observability/service_health.py` (수정)
- `src/pipeline/run_all.py` (수정)
- `tests/test_brief_fallback.py` (수정)
- `prompts/daily_brief.full.{system,user}.{txt,en.txt,ja.txt}` (신규)
- `prompts/daily_brief.incremental.{system,user}.{txt,en.txt,ja.txt}` (신규)
- `apps/dashboard/app/page.tsx`, `app/admin/page.tsx`
- `apps/dashboard/app/insights/insights.module.css`
- `apps/dashboard/components/curated-watchlist-panel.tsx`
- `apps/dashboard/components/whale-story-panel.tsx`
- `apps/dashboard/components/whale-story-detail-modal.module.css`
- `apps/dashboard/components/live-updates-controller.tsx`
- `apps/dashboard/components/admin/admin-operations-overview.tsx` (신규)
- `apps/dashboard/lib/render.ts` (신규)
- `apps/dashboard/lib/env.ts`, `lib/types.ts`, `lib/schema.ts`, `lib/metrics.ts`
- `.gitignore` (`data/brief_logs/` 추가)
